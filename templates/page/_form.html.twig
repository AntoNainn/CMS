{{ form_start(form) }}
    {# {{ form_widget(form) }} #}
    {{ form_row(form.titre) }}
    {{ form_row(form.contenu) }}
    {{ form_row(form.meta) }}
    {{ form_row(form.url) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
